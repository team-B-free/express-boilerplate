describe('test', () => {
  test('1+1', () => {
    expect(1 + 1).toBe(2);
  });

  test('1+2', () => {
    expect(1 + 2).toBe(3);
  });

  test('2+4', () => {
    expect(2 + 4).toBe(5);
  });

  test('3+4', () => {
    expect(3 + 4).toBe(6);
  });
});
